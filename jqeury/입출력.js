function f() {
    let a = 100;
    let b = 200;
    let as = a + b;

    let num = prompt("숫자를 입력하세요", "0");

    document.write(num - 5);

    /* <br> 태그 -> 줄바꿈 태그*/
    /*prompt -> 질의응답 창 , 입력받은 것을 data structure 에 저장가능*/

}


f();
